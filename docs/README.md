# docs/

Lightweight extras for the public GitHub presence.

- `index.html` — optional GitHub Pages landing (Settings → Pages → branch `main` / folder `/docs`). Points at Releases; not a marketing site.
- Put product screenshots in `screenshots/` when ready (see root README).

Release how-to for the maintainer lives in the project Context store as `docs/github-public-release.md`, not necessarily in this folder.
